const DATE_FORMAT = 'DD/MM/YYYY HH:mm:ss A';

const DEFAULT_PAGE_INDEX = 1;
const DEFAULT_LIMIT = 5;
const LIMIT_LIST = [{ name: 5 }, { name: 10 }, { name: 20 }, { name: 50 }];

module.exports = {
  DATE_FORMAT,
  DEFAULT_PAGE_INDEX,
  DEFAULT_LIMIT,
  LIMIT_LIST,
};
